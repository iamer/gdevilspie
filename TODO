* Finish GUI of rules creation dialog.
* Make main gdevilspie object that creates the main dialog, and create the rules editor dialog
when needed only.
* Refresh on rule creation dialog exit. Add refresh button to the rules list. Later on we can use inotify to automatcially refresh.
* Confirmation dialog for rule deletion.
* Fill the action and match selection trees.
* Change the properties notebook page on tree selection change in action and match trees.
* Add negation checkbox to match property notebook pages.
* Fill up relevant comboboxes with data from libwnck to allow for easy selection of properties of currently open windows.
* Button to retrieve data from a currently open window using target cursor.
* Add an optional alternate action notebook page, to allow for if -- then -- else rule creation.
* Possibililty to edit already available rules files. ( parser )
* Handle system wide rules, shown read only. Add unlock button for password entry to edit the system wide rules.
* Show deamon status and ability to start / stop it.
* tray icon, blinks when rule is matched.
* Help text all over the place.
* raw view of the to be generated rule.
* Text box at the top to name the rule / file.
